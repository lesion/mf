app
  .statusbar-overlay
  .views
    .view.view-main
      .navbar
        .navbar-inner
          .center.sliding { title }
      .pages.navbar-through
        testami
        altra

  script.
    this.title = 'ciao'
    console.log(this);

